android {
    signingConfigs {
        release {
            storeFile rootProject.file("atlascodecfix-release.jks")
            storePassword ""
            keyAlias ""
            keyPassword ""
        }
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.release
        }
    }
}
